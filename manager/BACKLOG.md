# Backlog

- add dedupe logic to prevent repeated appends
- add per-source ingestion modules
- add optional company blacklist scoring penalty
- add sponsorship-language parser with more nuance
- add tailored resume generator from a selected posting
- add cover-letter generator
- add cron or heartbeat integration
- add git commit automation when exec is healthy
